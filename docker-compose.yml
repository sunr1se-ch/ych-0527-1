version: '3.8'

services:
  salted-vegetable-dashboard:
    build: .
    container_name: salted-vegetable-dashboard
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
